# Ethos-U Doxygen sources

Build prerequisites, commands, folder layout, section ownership, and authoring
rules are maintained in the parent [documentation README](../README.md).

This directory contains the four manual sections (`general`, `vela`, `drivers`,
and `integration`), the shared style template, and `gen_doc.sh`.
